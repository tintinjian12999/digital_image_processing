To compile the c++ code
pls use
g++ -shared -fPIC -o main.dll main.cpp $(pkg-config --cflags --libs opencv4)
After compiling the c++ code. Run the code by 
python3 image_processing.py

Note: This works on Linux now. To work on Windows, pls modify line 9 in image_processing.py (To be tested)
